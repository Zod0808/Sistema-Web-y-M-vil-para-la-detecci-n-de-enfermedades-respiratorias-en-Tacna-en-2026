# =====================================================
# RespiCare — Entorno de producción VM
# IP: 38.250.158.233  |  Dominio: respicare.sytes.net
# =====================================================
# Copiar a .env antes de levantar:  cp .env.vm .env
# =====================================================

# ── MongoDB (contenedor local) ─────────────────────
MONGO_USERNAME=admin
MONGO_PASSWORD=CAMBIA_ESTA_PASSWORD_MONGO
MONGO_DB=respicare

# ── Redis ──────────────────────────────────────────
REDIS_PASSWORD=CAMBIA_ESTA_PASSWORD_REDIS

# ── JWT — genera con: openssl rand -base64 64 ─────
JWT_SECRET=CAMBIA_ESTE_SECRET_JWT
JWT_REFRESH_SECRET=CAMBIA_ESTE_REFRESH_SECRET_JWT
JWT_EXPIRE=7d
JWT_REFRESH_EXPIRE=30d

# ── CORS ───────────────────────────────────────────
# https://localhost  → Capacitor Android (androidScheme: 'https')
# capacitor://localhost → Capacitor fallback
# are-collecting-aids-females.trycloudflare.com → tunnel activo (actualizar si cambia)
CORS_ORIGINS=https://localhost,capacitor://localhost,http://38.250.158.233,http://respicare.sytes.net,https://respicare.sytes.net,https://are-collecting-aids-females.trycloudflare.com

# ── OpenAI (si el AI service lo usa) ──────────────
OPENAI_API_KEY=

# ── Email ──────────────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@respicare.sytes.net

# ── Logs ───────────────────────────────────────────
LOG_LEVEL=info
